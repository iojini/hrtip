services:
  - type: web
    name: hrtip-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn analyzer.api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
